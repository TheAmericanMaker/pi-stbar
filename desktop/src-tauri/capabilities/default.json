{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Default capability for the pi-stbar desktop window. Native FS + shell are app-defined commands (no ACL needed); the HTTP plugin routes provider/Ollama requests through Rust to bypass browser CORS.",
	"windows": ["main"],
	"permissions": [
		"core:default",
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://localhost:*" },
				{ "url": "http://127.0.0.1:*" },
				{ "url": "http://**" },
				{ "url": "https://**" }
			]
		}
	]
}
