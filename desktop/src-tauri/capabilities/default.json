{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Default capability for the pi-stbar desktop window. Native FS + shell are app-defined commands and need no ACL permissions.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
